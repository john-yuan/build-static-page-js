module.exports = {
  // Path to ignore, relative to the directory of config file.
  // If the path is absolute, the path will be ignored.
  ignore: [
    '.buildStaticPage.config.js'
  ],
  // Filename to ignore recursively.
  ignoreRecursively: [
    '.DS_Store',
    'node_modules'
  ],
};
